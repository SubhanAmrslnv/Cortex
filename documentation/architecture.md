# Architecture

## System overview

Cortex is structured as three independent layers: a guard layer that fires before tool execution, a runtime layer that fires after, and a command layer that orchestrates analysis workflows. All layers read their configuration from a central JSON registry under `.claude/registry/`. The `.claude/` directory serves as both the Claude Code adapter layer (wiring hook script paths to events) and the framework root — there is no separate `.cortex/` directory. The framework root (`CORTEX_ROOT`) is resolved dynamically at runtime so that Cortex can be installed globally, per-project, or via environment variable without any path hardcoding.

## Directory layout

```
.claude/
  settings.json                 ← adapter: wires hooks to Claude Code events
  settings.local.json           ← local permission overrides (not committed)
  keybindings.json              ← key binding customizations
  commands/                     ← full command implementations (commit.md, debug.md, …)
  core/
    hooks/
      guards/
        pre-guard.sh              ← PreToolUse risk-scoring engine (v2.3.0)
        permission-request.sh     ← PermissionRequest intent enricher (v1.1.1)
        permission-denied.sh      ← PermissionDenied recovery suggester (v1.1.1)
      runtime/
        post-format.sh            ← registry-driven formatter dispatcher (v2.4.0)
        post-scan.sh              ← registry-driven security scanner dispatcher (v2.5.0)
        post-audit-log.sh         ← audit log appender (v1.2.0)
        post-code-intel.sh        ← code intelligence analyzer (v1.2.0)
        post-error-analyzer.sh    ← tool failure classifier (v1.1.0)
        notification.sh           ← notification aggregator (v1.0.1)
        task-tracker.sh           ← task lifecycle persister (v1.0.2)
        stop-build.sh             ← Stop hook: skip if running, retry 3x, report failures (v1.4.0)
        session-start.sh          ← project profiler (v1.2.0)
        prompt-optimizer.sh       ← UserPromptSubmit context injector (v1.6.0)
    runtime/
      command-runner.sh           ← validates and dispatches slash commands
    scanners/
      dotnet/                     ← .cs
      node/                       ← .ts .tsx .js .jsx .vue .svelte
      python/                     ← .py
      java/                       ← .java
      go/                         ← .go
      rust/                       ← .rs
      bash/                       ← .sh .bash
      generic/                    ← secret scan for all file types (*)
      ...                         ← 17 additional language scanners
  registry/
    hooks.json                    ← hook names, versions, source paths
    commands.json                 ← command registry
    scanners.json                 ← extension → scanner array mapping
  config/
    cortex.config.json            ← framework version (3.1.0) and runtime paths
  cache/
    project-profile.json          ← session-start output; consumed by prompt-optimizer
    scans/                        ← hash-based scan cache; entries pruned after 7 days
  state/
    snapshot.json                 ← regression baseline (generated by /regression --save)
    index.json                    ← snapshot history
  test/
    run.sh                        ← smoke test runner
    fixtures/                     ← JSON payloads for pre-guard, post-error-analyzer, post-scan
  base/                           ← remote framework snapshot (updated by /update-cortex)
  local/                          ← project-local overrides (never overwritten)
```

## Layer responsibilities

| Layer | Path | Responsibility |
|---|---|---|
| Guards | `core/hooks/guards/` | Gate logic only — block or inform before/after permission events |
| Runtime hooks | `core/hooks/runtime/` | Execution only — run after tools complete |
| Scanners | `core/scanners/` | Analysis only — called by `post-scan.sh` and `post-format.sh` |
| Commands | `commands/` | Orchestration only — no inline business logic |
| Registry | `registry/` | Configuration only — no executable code |
| Adapter | `settings.json` | Wiring only — no business logic |
| Cache | `cache/` | Generated state — read by prompt-optimizer, written by session-start |
| State | `state/` | Persistent snapshots — written by `/regression --save`, read by `/regression` |
| Local overrides | `local/` | Project customization — never modified by framework updates |

## Separation of concerns

- **Guards** may only block or enrich permission events. They must not write files or invoke scanners.
- **Runtime hooks** are read-only except for the audit log. They analyze and output structured JSON; they never modify the files they inspect.
- **Scanners** are called by `post-scan.sh` and `post-format.sh` via registry dispatch. They must not invoke other hooks.
- **Commands** read from the project and registry; they may invoke Bash tools and write reports, but they must not modify hook or registry files directly.
- **The adapter** (`settings.json`) contains only hook event wiring. All logic lives in `core/`.

## CORTEX_ROOT resolution

Every hook and command resolves its runtime path dynamically. Resolution order:

1. `$CORTEX_ROOT` environment variable — used in CI, Docker, and custom installs
2. `$(pwd)/.claude` — project-local install detected at session start
3. `$HOME/.claude` — global install fallback

Every hook script contains this block near the top:

```bash
if [ -z "$CORTEX_ROOT" ]; then
  if [ -d "$(pwd)/.claude" ]; then
    export CORTEX_ROOT="$(pwd)/.claude"
  else
    export CORTEX_ROOT="$HOME/.claude"
  fi
fi
```

`settings.json` uses `${CORTEX_ROOT:-$(pwd)/.claude}/core/hooks/...` for all hook paths.

## Key design decisions

- **Registry-driven dispatch** — `post-scan.sh` and `post-format.sh` read `scanners.json` at runtime, so adding a new scanner requires only a registry entry and a script file; no hook changes needed
- **Numeric risk scoring** — `pre-guard.sh` accumulates points across 6 independent categories rather than pattern-matching against a blocklist; this allows partial matches to compound and intermediate-risk commands to warn rather than block
- **Idempotent session profiling** — `session-start.sh` fingerprints project files and skips rewriting the profile cache if nothing changed, keeping session startup fast
- **Snapshot-based regression** — `/regression` stores a fingerprinted snapshot of `/doctor` output and diffs against it, giving stable cross-session issue comparison without requiring external state
- **No hardcoded paths** — `settings.json` uses `${CORTEX_ROOT:-$(pwd)/.claude}/...` for every hook path, making the framework fully relocatable
- **Unified directory** — the framework and Claude Code adapter both live in `.claude/`, eliminating the need to manage two separate directories (`.claude/` + `.cortex/`)
